# This requirements file lists all third-party dependencies for this project.
#
# Run 'pip install -r requirements.txt -t ext/' to install these dependencies
# in `ext/` subdirectory.
#
# Note: The `ext` directory is added to `sys.path` by `appengine_config.py`.
Flask==0.10.1
simplejson==3.7.2